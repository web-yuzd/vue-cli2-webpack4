const state = {
  sidebar: false
};

const getters = {

};

const mutations = {
  'SIDEBAR'(state, status) {
    state.sidebar = status;
  }
};

const actions = {

};

export default {
  state,
  getters,
  mutations,
  actions
}
